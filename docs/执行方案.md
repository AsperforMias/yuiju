# BehaviorRecord 数据表改造执行方案

## 概述

本文档详细说明如何将现有的 `ActionSchema` 直接替换为 `BehaviorRecord`，实现更完善的行为记录功能。

> **注意**：项目当前处于开发阶段，无需考虑历史兼容性，将直接替换旧 Schema。

## 改造目标

1. 将 `ActionSchema` 完全替换为 `BehaviorRecord`
2. 支持记录行为参数（`parameters`）
3. 支持多种触发来源（`trigger`：agent/user/system）
4. 支持行为持续时间（`duration_minutes`）
5. 删除所有与旧 `ActionSchema` 相关的代码

## 改造步骤

### 步骤 1：直接替换 action.schema.ts

**文件位置**：`packages/utils/src/db/schema/action.schema.ts`

**操作**：完全替换文件内容

**内容要点**：

- 定义 `BehaviorParameter` 类型
- 定义 `IBehaviorRecord` 接口（替代原 `IActionSchema`）
- 创建 `BehaviorRecordSchema`（替代原 `ActionSchema`）
- 添加复合索引优化查询性能
- 更新导出函数名称：`saveAction` → `saveBehaviorRecord`，`getRecentActions` → `getRecentBehaviorRecords`

**关键代码结构**：

```typescript
/**
 * 行为参数
 * 用于存储 ParameterAgent 选择的具体参数
 */
export interface BehaviorParameter {
  /** 参数值，如："苹果"、"面包" */
  value: string;
  /** 数量，默认为 1 */
  quantity?: number;
  /** 参数描述，如："苹果可以恢复15点体力" */
  description: string;
  /** 额外信息，如：{ stamina: 15, price: 5 } */
  extra?: Record<string, any>;
}

/**
 * 行为记录接口
 */
export interface IBehaviorRecord extends Document {
  /** 行为/事件类型 */
  behavior: string;
  /** 行为描述 */
  description: string;
  /** 发生时间 */
  timestamp: Date;
  /** 触发来源：agent（LLM）、user（用户）、system（系统） */
  trigger: 'agent' | 'user' | 'system';
  /** Agent 选择的行为参数 */
  parameters?: BehaviorParameter[];
  /** 行为持续时间（分钟） */
  duration_minutes?: number;
}

// 定义 Schema
const BehaviorRecordSchema = new Schema<IBehaviorRecord>({
  behavior: { type: String, required: true, index: true },
  description: { type: String, required: true },
  timestamp: { type: Date, required: true, default: Date.now, index: true },
  trigger: {
    type: String,
    enum: ['agent', 'user', 'system'],
    required: true,
    default: 'agent',
    index: true,
  },
  parameters: [
    {
      value: { type: String, required: true },
      quantity: { type: Number, default: 1 },
      description: { type: String, required: true },
      extra: { type: Schema.Types.Mixed, default: {} },
    },
  ],
  duration_minutes: { type: Number },
});

// 复合索引
BehaviorRecordSchema.index({ timestamp: -1 });
BehaviorRecordSchema.index({ trigger: 1, timestamp: -1 });
BehaviorRecordSchema.index({ behavior: 1, timestamp: -1 });

export const BehaviorRecordModel = mongoose.model<IBehaviorRecord>('BehaviorRecord', BehaviorRecordSchema);

// 导出函数
export async function saveBehaviorRecord(behaviorData: Partial<IBehaviorRecord>) {
  const behavior = new BehaviorRecordModel(behaviorData);
  return await behavior.save();
}

export async function getRecentBehaviorRecords(limit: number = 10) {
  return await BehaviorRecordModel.find().sort({ timestamp: -1 }).limit(limit).exec();
}
```

**检查位置**：`packages/utils/src/index.ts` 或 `packages/utils/src/db/index.ts`

**操作**：确认导出路径正确（通常不需要修改，因为文件名没变）

### 步骤 3：修改 tick.ts 中的保存逻辑

**文件位置**：`packages/world/src/engine/tick.ts`

**修改点 1**：更新导入语句（第 9 行）

```typescript
// 替换
import { getRecentActions, saveAction } from '@yuiju/utils';
// 为
import { getRecentBehaviorRecords, saveBehaviorRecord } from '@yuiju/utils';
```

**修改点 2**：更新历史记录查询（第 59-64 行）

```typescript
// 替换
const recentActions = await getRecentActions(10);
const history = recentActions.map(a => ({
  action: a.action_id as ActionId,
  reason: a.reason,
  timestamp: a.create_time.getTime(),
}));
// 为
const recentBehaviors = await getRecentBehaviorRecords(10);
const history = recentBehaviors.map(b => ({
  action: b.behavior as ActionId,
  reason: b.description,
  timestamp: b.timestamp.getTime(),
}));
```

**修改点 3**：更新保存逻辑（第 85-91 行）

**当前代码问题**：`durationMin` 在保存之后才计算，需要调整代码顺序。

**修改后逻辑**：

```typescript
// 1. 先计算持续时间（从第 96-101 行提前）
const durationMin = await getDurationTime(
  actionMetadata.durationMin,
  context,
  selectedAction.durationMinute,
  selectedParameter?.parameters
);

// 2. 保存行为记录
if (isProd) {
  await saveBehaviorRecord({
    behavior: selectedAction.action,
    description: selectedAction.reason,
    timestamp: new Date(),
    trigger: 'agent',
    parameters: selectedParameter?.parameters?.map(p => ({
      value: p.value,
      quantity: p.quantity ?? 1,
      description: p.description,
      extra: p.extra,
    })),
    duration_minutes: durationMin,
  });
}
```

**完整代码结构调整**：

```typescript
// 执行行为
await actionMetadata.executor(context, selectedParameter?.parameters);

// 更新世界时间（第一次）
await context.worldState.updateTime();

// 计算行为持续时间
const durationMin = await getDurationTime(
  actionMetadata.durationMin,
  context,
  selectedAction.durationMinute,
  selectedParameter?.parameters
);

// 保存行为记录（包含持续时间）
if (isProd) {
  await saveBehaviorRecord({
    behavior: selectedAction.action,
    description: selectedAction.reason,
    timestamp: new Date(),
    trigger: 'agent',
    parameters: selectedParameter?.parameters?.map(p => ({
      value: p.value,
      quantity: p.quantity ?? 1,
      description: p.description,
      extra: p.extra,
    })),
    duration_minutes: durationMin,
  });
}

// 生成完成事件
const completionEvent =
  typeof actionMetadata.completionEvent === 'function'
    ? await actionMetadata.completionEvent(context, selectedParameter?.parameters)
    : actionMetadata.completionEvent;
```

### 步骤 4：更新 ActionRecord 类型定义

**文件位置**：`packages/world/src/types/action.ts`

**修改点**：更新 `ActionRecord` 接口（第 110-114 行）

```typescript
// 替换
export interface ActionRecord {
  action: ActionId;
  reason: string;
  timestamp: number;
}
// 为
export interface ActionRecord {
  behavior: ActionId; // 改为 behavior，与数据库字段一致
  description: string; // 改为 description
  timestamp: number;
}
```

**说明**：与数据库 Schema 保持语义一致性，便于后续添加 `trigger` 等字段。

### 步骤 5：检查并更新其他引用

**检查点 1**：`packages/world/src/llm/coordinator.ts`

**说明**：

- 该文件使用 `ActionRecord` 类型
- 接口更新后，代码逻辑保持兼容，无需修改
- 注意：coordinator 内部使用的是 `action` 和 `reason` 字段名映射

**验证**：确保代码中访问 `behavior` 和 `description` 字段时不会报错。

**检查点 2**：全局搜索可能的引用

使用以下命令搜索是否有其他地方引用了旧的接口或函数：

```bash
# 搜索 IActionSchema
grep -r "IActionSchema" packages/

# 搜索 saveAction
grep -r "saveAction" packages/

# 搜索 getRecentActions
grep -r "getRecentActions" packages/

# 搜索 action_id
grep -r "action_id" packages/
```

**操作**：根据搜索结果，逐个更新所有引用。

## MongoDB 集合变化

**旧集合**：`actions`
**新集合**：`behaviorrecords`

**注意事项**：

- 如果本地数据库已有 `actions` 集合的数据，可以手动删除
- MongoDB 会自动创建 `behaviorrecords` 集合
- 无需手动迁移数据（开发阶段）

## 注意事项

1. **字段命名一致性**：

   - `behavior` 而非 `action_id`
   - `description` 而非 `reason`
   - `timestamp` 而非 `create_time`

2. **duration_minutes 计算时机**：

   - 需要在保存前完成计算
   - 已调整代码顺序，确保传入正确的 `parameters`

3. **触发来源默认值**：

   - Agent 行为使用 `'agent'`
   - 用户操作（如给金币）使用 `'user'`
   - 系统事件（如每日签到）使用 `'system'`

4. **TypeScript 类型安全**：
   - 确保所有使用 `ActionRecord` 的地方更新为新字段名
   - 注意 `behavior` 和 `description` 的映射关系

## 实施检查清单

- [ ] 替换 `action.schema.ts` 为新的 `BehaviorRecord` Schema
- [ ] 更新 `tick.ts` 导入语句
- [ ] 调整 `tick.ts` 代码顺序，先计算 `durationMin` 再保存
- [ ] 更新 `tick.ts` 保存逻辑，包含 `parameters` 和 `duration_minutes`
- [ ] 更新 `tick.ts` 历史记录查询逻辑
- [ ] 更新 `ActionRecord` 类型定义（`action.ts`）
- [ ] 全局搜索并更新所有旧接口引用
- [ ] （可选）添加用户和系统行为记录接口
- [ ] 运行类型检查：`pnpm --filter @yuiju/world run type-check`
- [ ] 运行代码检查：`pnpm check`
